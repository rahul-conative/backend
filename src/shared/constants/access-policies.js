const { ROLES } = require("./roles");
const { CAPABILITIES } = require("./capabilities");

const ROLE_CAPABILITIES = {
  [ROLES.ADMIN]: ["*"],
  [ROLES.SUB_ADMIN]: ["*"],
  [ROLES.SELLER]: [
    CAPABILITIES.CATALOG_MANAGE,
    CAPABILITIES.SELLER_KYC_SUBMIT,
    CAPABILITIES.ORDER_MANAGE,
    CAPABILITIES.SELLER_DASHBOARD_VIEW,
    CAPABILITIES.SELLER_PROFILE_MANAGE,
  ],
  [ROLES.BUYER]: [
    CAPABILITIES.PROFILE_SELF,
    CAPABILITIES.CART_SELF,
    CAPABILITIES.ORDER_SELF,
    CAPABILITIES.PAYMENT_SELF,
    CAPABILITIES.WALLET_SELF,
    CAPABILITIES.USER_KYC_SUBMIT,
  ],
  [ROLES.SELLER_SUB_ADMIN]: [
    CAPABILITIES.CATALOG_MANAGE,
    CAPABILITIES.ORDER_MANAGE,
    CAPABILITIES.SELLER_DASHBOARD_VIEW,
    CAPABILITIES.SELLER_PROFILE_MANAGE,
  ],
  [ROLES.SUPER_ADMIN]: ["*"],
};

module.exports = { ROLE_CAPABILITIES };
